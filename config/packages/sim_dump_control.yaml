# Dump-load CONTROL (HA owns on/off). Opt-in package for Home Assistant on .105.
# Requires sim_dump_plugs.yaml (switch.sim_ac_plug_*).
# Victron float hold then staged ON (site load delta, 15 min min-on, 10 min cooldown).
# Solar-gone and re-bulk OFF (1 minute).
# Surplus watts (dump_surplus_w) are briefing only -- not the ON/OFF trigger.
# Official:
#   https://www.home-assistant.io/docs/configuration/packages/
#   https://www.home-assistant.io/docs/automation/yaml/
#   https://www.home-assistant.io/integrations/threshold/
#   https://www.home-assistant.io/docs/automation/trigger/
#   https://www.home-assistant.io/integrations/timer/
#   https://www.home-assistant.io/integrations/derivative/
#   https://www.home-assistant.io/integrations/template/
#   https://www.home-assistant.io/integrations/switch/
#   https://www.home-assistant.io/integrations/input_number/
#   https://www.home-assistant.io/integrations/input_select/
#   https://www.home-assistant.io/docs/scripts/
#   https://www.home-assistant.io/docs/automation/trigger/#template-trigger
#   https://www.home-assistant.io/triggers/homeassistant/
#   https://www.home-assistant.io/triggers/event/
#   https://www.home-assistant.io/docs/configuration/events/
# Victron 1-minute re-bulk:
#   https://www.victronenergy.com/media/pg/Manual_BlueSolar_MPPT_75-10_up_to_100-20/en/operation.html
# SmartShunt +charge / -discharge:
#   https://www.victronenergy.com/media/pg/SmartShunt/en/operation.html

input_boolean:
  dump_control_enabled:
    name: Dump load HA control
    icon: mdi:transmission-tower-export
  dump_soc_unsynced:
    name: Dump SoC unsynced
    icon: mdi:battery-unknown
    initial: true

input_number:
  dump_ac_limit_t2_w:
    name: T2 Renogy AC limit
    min: 500
    max: 4000
    step: 50
    initial: 2000
    unit_of_measurement: W
    mode: box
    icon: mdi:current-ac
  dump_ac_limit_ku_w:
    name: KU Renogy AC limit
    min: 500
    max: 4000
    step: 50
    initial: 2000
    unit_of_measurement: W
    mode: box
    icon: mdi:current-ac
  dump_ac_limit_sph_w:
    name: SPH AC limit
    min: 500
    max: 4000
    step: 50
    initial: 2000
    unit_of_measurement: W
    mode: box
    icon: mdi:current-ac
  dump_max_discharge_t2_w:
    name: T2 max battery discharge
    min: 0
    max: 500
    step: 10
    initial: 0
    unit_of_measurement: W
    mode: box
    icon: mdi:battery-minus
  dump_max_discharge_ku_w:
    name: KU max battery discharge
    min: 0
    max: 500
    step: 10
    initial: 0
    unit_of_measurement: W
    mode: box
    icon: mdi:battery-minus
  dump_max_discharge_sph_w:
    name: SPH max battery discharge
    min: 0
    max: 500
    step: 10
    initial: 50
    unit_of_measurement: W
    mode: box
    icon: mdi:battery-minus
  dump_float_t2_v:
    name: T2 float volts
    min: 26.0
    max: 29.5
    step: 0.05
    initial: 27.0
    unit_of_measurement: V
    mode: box
    icon: mdi:sine-wave
  dump_rebulk_t2_v:
    name: T2 re-bulk volts
    min: 25.5
    max: 29.0
    step: 0.05
    initial: 26.8
    unit_of_measurement: V
    mode: box
    icon: mdi:sine-wave
  dump_float_ku_v:
    name: KU float volts
    min: 26.0
    max: 29.5
    step: 0.05
    initial: 27.0
    unit_of_measurement: V
    mode: box
    icon: mdi:sine-wave
  dump_rebulk_ku_v:
    name: KU re-bulk volts
    min: 25.5
    max: 29.0
    step: 0.05
    initial: 26.8
    unit_of_measurement: V
    mode: box
    icon: mdi:sine-wave
  dump_float_sph_v:
    name: SPH float volts
    min: 26.0
    max: 29.5
    step: 0.05
    initial: 27.0
    unit_of_measurement: V
    mode: box
    icon: mdi:sine-wave
  dump_rebulk_sph_v:
    name: SPH re-bulk volts
    min: 25.5
    max: 29.0
    step: 0.05
    initial: 26.8
    unit_of_measurement: V
    mode: box
    icon: mdi:sine-wave
  dump_min_solar_w:
    name: Dump min solar
    min: 0
    max: 500
    step: 10
    initial: 50
    unit_of_measurement: W
    mode: box
    icon: mdi:solar-power
  dump_min_soc_percent:
    name: Dump min SoC
    min: 80
    max: 100
    step: 1
    initial: 95
    unit_of_measurement: "%"
    mode: box
    icon: mdi:battery-90
  dump_site_confirm_s:
    name: Dump site confirm seconds
    min: 5
    max: 30
    step: 1
    initial: 5
    unit_of_measurement: s
    mode: box
    icon: mdi:timer-sand
  dump_site_delta_min_w:
    name: Dump site delta min watts
    min: 5
    max: 500
    step: 5
    initial: 25
    unit_of_measurement: W
    mode: box
    icon: mdi:transmission-tower

input_select:
  dump_plug_1_inverter:
    name: Plug 1 inverter
    options:
      - T2
      - KU
      - SPH
    initial: SPH
    icon: mdi:transmission-tower
  dump_plug_2_inverter:
    name: Plug 2 inverter
    options:
      - T2
      - KU
      - SPH
    initial: SPH
    icon: mdi:transmission-tower
  dump_plug_3_inverter:
    name: Plug 3 inverter
    options:
      - T2
      - KU
      - SPH
    initial: SPH
    icon: mdi:transmission-tower
  dump_plug_4_inverter:
    name: Plug 4 inverter
    options:
      - T2
      - KU
      - SPH
    initial: SPH
    icon: mdi:transmission-tower
  dump_plug_5_inverter:
    name: Plug 5 inverter
    options:
      - T2
      - KU
      - SPH
    initial: SPH
    icon: mdi:transmission-tower
  dump_plug_6_inverter:
    name: Plug 6 inverter
    options:
      - T2
      - KU
      - SPH
    initial: SPH
    icon: mdi:transmission-tower

timer:
  dump_plug_1_min_on:
    name: Dump plug 1 min on
    duration: "00:15:00"
    restore: true
  dump_plug_1_cooldown:
    name: Dump plug 1 cooldown
    duration: "00:10:00"
    restore: true
  dump_plug_2_min_on:
    name: Dump plug 2 min on
    duration: "00:15:00"
    restore: true
  dump_plug_2_cooldown:
    name: Dump plug 2 cooldown
    duration: "00:10:00"
    restore: true
  dump_plug_3_min_on:
    name: Dump plug 3 min on
    duration: "00:15:00"
    restore: true
  dump_plug_3_cooldown:
    name: Dump plug 3 cooldown
    duration: "00:10:00"
    restore: true
  dump_plug_4_min_on:
    name: Dump plug 4 min on
    duration: "00:15:00"
    restore: true
  dump_plug_4_cooldown:
    name: Dump plug 4 cooldown
    duration: "00:10:00"
    restore: true
  dump_plug_5_min_on:
    name: Dump plug 5 min on
    duration: "00:15:00"
    restore: true
  dump_plug_5_cooldown:
    name: Dump plug 5 cooldown
    duration: "00:10:00"
    restore: true
  dump_plug_6_min_on:
    name: Dump plug 6 min on
    duration: "00:15:00"
    restore: true
  dump_plug_6_cooldown:
    name: Dump plug 6 cooldown
    duration: "00:10:00"
    restore: true

template:
  - sensor:
      - name: Dump surplus
        unique_id: dump_surplus_w
        default_entity_id: sensor.dump_surplus_w
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        state: >
          {% set solar_live = states('sensor.solar_controller_solar') %}
          {% set solar_canon = states('sensor.solar_controller_solar_power') %}
          {% set solar = solar_live if is_number(solar_live) else solar_canon %}
          {% set load = states('sensor.sim_dump_load_power') %}
          {% set losses = states('sensor.solar_component_losses_power') %}
          {% if not is_number(solar) %}
            {{ none }}
          {% else %}
            {{ (solar | float(0)) - (load | float(0)) - (losses | float(0)) }}
          {% endif %}
      - name: Dump bus load T2
        unique_id: dump_bus_load_t2
        default_entity_id: sensor.dump_bus_load_t2
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        state: >
          {% set p = states('sensor.battery_1_power') %}
          {{ (-1 * (p | float)) if is_number(p) else none }}
      - name: Dump bus load KU
        unique_id: dump_bus_load_ku
        default_entity_id: sensor.dump_bus_load_ku
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        state: >
          {% set p = states('sensor.battery_2_power') %}
          {{ (-1 * (p | float)) if is_number(p) else none }}
      - name: Dump bus load SPH
        unique_id: dump_bus_load_sph
        default_entity_id: sensor.dump_bus_load_sph
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        state: >
          {% set p = states('sensor.sungold_sph302480a_load_power') %}
          {{ (p | float) if is_number(p) else none }}
      - name: Dump next plug
        unique_id: dump_next_plug
        default_entity_id: sensor.dump_next_plug
        icon: mdi:power-socket-us
        state: >
          {% set plugs = [
            ('switch.sim_ac_plug_1', 'sensor.sim_ac_plug_1_power', 'sensor.dump_plug_1_last_w', 'input_select.dump_plug_1_inverter'),
            ('switch.sim_ac_plug_2', 'sensor.sim_ac_plug_2_power', 'sensor.dump_plug_2_last_w', 'input_select.dump_plug_2_inverter'),
            ('switch.sim_ac_plug_3', 'sensor.sim_ac_plug_3_power', 'sensor.dump_plug_3_last_w', 'input_select.dump_plug_3_inverter'),
            ('switch.sim_ac_plug_4', 'sensor.sim_ac_plug_4_power', 'sensor.dump_plug_4_last_w', 'input_select.dump_plug_4_inverter'),
            ('switch.sim_ac_plug_5', 'sensor.sim_ac_plug_5_power', 'sensor.dump_plug_5_last_w', 'input_select.dump_plug_5_inverter'),
            ('switch.sim_ac_plug_6', 'sensor.sim_ac_plug_6_power', 'sensor.dump_plug_6_last_w', 'input_select.dump_plug_6_inverter'),
          ] %}
          {% set lim_t2 = states('input_number.dump_ac_limit_t2_w') | float(2000) %}
          {% set lim_ku = states('input_number.dump_ac_limit_ku_w') | float(2000) %}
          {% set lim_sph = states('input_number.dump_ac_limit_sph_w') | float(2000) %}
          {% set sg = states('sensor.sungold_sph302480a_load_power') | float(0)
             if is_number(states('sensor.sungold_sph302480a_load_power')) else 0 %}
          {% set t2_pack = states('sensor.battery_1_power') | float(0)
             if is_number(states('sensor.battery_1_power')) else 0 %}
          {% set ku_pack = states('sensor.battery_2_power') | float(0)
             if is_number(states('sensor.battery_2_power')) else 0 %}
          {% set t2_from_batt = [0, -t2_pack] | max %}
          {% set ku_from_batt = [0, -ku_pack] | max %}
          {% set ns = namespace(t2=0, ku=0, sph=0) %}
          {% for eid, pwr, last_eid, sel in plugs %}
            {% if is_state(eid, 'on') and is_number(states(pwr)) %}
              {% set live = states(pwr) | float(0) %}
              {% set bus = states(sel) %}
              {% if bus == 'T2' %}{% set ns.t2 = ns.t2 + live %}
              {% elif bus == 'KU' %}{% set ns.ku = ns.ku + live %}
              {% else %}{% set ns.sph = ns.sph + live %}
              {% endif %}
            {% endif %}
          {% endfor %}
          {% set head = {
            'T2': lim_t2 - ns.t2 - t2_from_batt,
            'KU': lim_ku - ns.ku - ku_from_batt,
            'SPH': lim_sph - sg - ns.sph,
          } %}
          {% set batt = {
            'T2': is_state('binary_sensor.dump_batt_t2_ok', 'on'),
            'KU': is_state('binary_sensor.dump_batt_ku_ok', 'on'),
            'SPH': is_state('binary_sensor.dump_batt_sph_ok', 'on'),
          } %}
          {% set vfloat = {
            'T2': is_state('binary_sensor.dump_v_float_t2', 'on'),
            'KU': is_state('binary_sensor.dump_v_float_ku', 'on'),
            'SPH': is_state('binary_sensor.dump_v_float_sph', 'on'),
          } %}
          {% set vrebulk = {
            'T2': is_state('binary_sensor.dump_v_rebulk_t2', 'on'),
            'KU': is_state('binary_sensor.dump_v_rebulk_ku', 'on'),
            'SPH': is_state('binary_sensor.dump_v_rebulk_sph', 'on'),
          } %}
          {% if not is_state('binary_sensor.dump_solar_present', 'on')
             or not is_state('binary_sensor.dump_charge_float', 'on')
             or not is_state('binary_sensor.dump_soc_ok', 'on') %}
            none
          {% else %}
            {% set pick = namespace(eid='none', probe='none') %}
            {% set built = namespace(rows=[]) %}
            {% for eid, pwr, last_eid, sel in plugs %}
              {% set last_raw = states(last_eid) %}
              {% set last_w = last_raw | float(0) if is_number(last_raw) else none %}
              {% set sort_w = last_w if last_w is not none else 99999 %}
              {% set built.rows = built.rows + [(sort_w, eid, sel, last_w)] %}
            {% endfor %}
            {% for sort_w, eid, sel, last_w in built.rows | sort %}
              {% set plug_n = eid.split('_')[-1] %}
              {% set cd = 'timer.dump_plug_' ~ plug_n ~ '_cooldown' %}
              {% set bus = states(sel) %}
              {% if is_state(eid, 'off') and is_state(cd, 'idle')
                 and batt.get(bus, False)
                 and vfloat.get(bus, False) and not vrebulk.get(bus, False) %}
                {% if last_w is not none and last_w <= head.get(bus, -1) %}
                  {% if pick.eid == 'none' %}
                    {% set pick.eid = eid %}
                  {% endif %}
                {% elif last_w is none and pick.probe == 'none' and head.get(bus, -1) > 0 %}
                  {% set pick.probe = eid %}
                {% endif %}
              {% endif %}
            {% endfor %}
            {{ pick.eid if pick.eid != 'none' else pick.probe }}
          {% endif %}
        attributes:
          surplus_w: "{{ states('sensor.dump_surplus_w') | float(0) if is_number(states('sensor.dump_surplus_w')) else none }}"
          sungold_load_w: "{{ states('sensor.sungold_sph302480a_load_power') | float(0) if is_number(states('sensor.sungold_sph302480a_load_power')) else 0 }}"
          t2_pack_w: "{{ states('sensor.battery_1_power') | float(0) if is_number(states('sensor.battery_1_power')) else none }}"
          ku_pack_w: "{{ states('sensor.battery_2_power') | float(0) if is_number(states('sensor.battery_2_power')) else none }}"
          dump_load_w: "{{ states('sensor.sim_dump_load_power') | float(0) if is_number(states('sensor.sim_dump_load_power')) else 0 }}"
          charge_float: "{{ is_state('binary_sensor.dump_charge_float', 'on') }}"
          solar_present: "{{ is_state('binary_sensor.dump_solar_present', 'on') }}"
  - trigger:
      - trigger: state
        entity_id: sensor.sim_ac_plug_1_power
    sensor:
      - name: Dump plug 1 last watts
        unique_id: dump_plug_1_last_w
        default_entity_id: sensor.dump_plug_1_last_w
        unit_of_measurement: W
        device_class: power
        state: >
          {% if is_number(trigger.to_state.state) and (trigger.to_state.state | float) > 0 %}
            {{ trigger.to_state.state | float }}
          {% else %}
            {{ this.state if this is defined and is_number(this.state) else none }}
          {% endif %}
  - trigger:
      - trigger: state
        entity_id: sensor.sim_ac_plug_2_power
    sensor:
      - name: Dump plug 2 last watts
        unique_id: dump_plug_2_last_w
        default_entity_id: sensor.dump_plug_2_last_w
        unit_of_measurement: W
        device_class: power
        state: >
          {% if is_number(trigger.to_state.state) and (trigger.to_state.state | float) > 0 %}
            {{ trigger.to_state.state | float }}
          {% else %}
            {{ this.state if this is defined and is_number(this.state) else none }}
          {% endif %}
  - trigger:
      - trigger: state
        entity_id: sensor.sim_ac_plug_3_power
    sensor:
      - name: Dump plug 3 last watts
        unique_id: dump_plug_3_last_w
        default_entity_id: sensor.dump_plug_3_last_w
        unit_of_measurement: W
        device_class: power
        state: >
          {% if is_number(trigger.to_state.state) and (trigger.to_state.state | float) > 0 %}
            {{ trigger.to_state.state | float }}
          {% else %}
            {{ this.state if this is defined and is_number(this.state) else none }}
          {% endif %}
  - trigger:
      - trigger: state
        entity_id: sensor.sim_ac_plug_4_power
    sensor:
      - name: Dump plug 4 last watts
        unique_id: dump_plug_4_last_w
        default_entity_id: sensor.dump_plug_4_last_w
        unit_of_measurement: W
        device_class: power
        state: >
          {% if is_number(trigger.to_state.state) and (trigger.to_state.state | float) > 0 %}
            {{ trigger.to_state.state | float }}
          {% else %}
            {{ this.state if this is defined and is_number(this.state) else none }}
          {% endif %}
  - trigger:
      - trigger: state
        entity_id: sensor.sim_ac_plug_5_power
    sensor:
      - name: Dump plug 5 last watts
        unique_id: dump_plug_5_last_w
        default_entity_id: sensor.dump_plug_5_last_w
        unit_of_measurement: W
        device_class: power
        state: >
          {% if is_number(trigger.to_state.state) and (trigger.to_state.state | float) > 0 %}
            {{ trigger.to_state.state | float }}
          {% else %}
            {{ this.state if this is defined and is_number(this.state) else none }}
          {% endif %}
  - trigger:
      - trigger: state
        entity_id: sensor.sim_ac_plug_6_power
    sensor:
      - name: Dump plug 6 last watts
        unique_id: dump_plug_6_last_w
        default_entity_id: sensor.dump_plug_6_last_w
        unit_of_measurement: W
        device_class: power
        state: >
          {% if is_number(trigger.to_state.state) and (trigger.to_state.state | float) > 0 %}
            {{ trigger.to_state.state | float }}
          {% else %}
            {{ this.state if this is defined and is_number(this.state) else none }}
          {% endif %}
  - binary_sensor:
      - name: Dump charge ok
        unique_id: dump_charge_ok
        default_entity_id: binary_sensor.dump_charge_ok
        state: >
          {% set a = states('sensor.solar_controller_charge_state') | lower %}
          {% set b = states('sensor.solar_controller_battery_state') | lower %}
          {{ a in ['absorption', 'float'] or b in ['absorption', 'float'] }}
      - name: Dump batt T2 ok
        unique_id: dump_batt_t2_ok
        default_entity_id: binary_sensor.dump_batt_t2_ok
        state: >
          {% set p = states('sensor.battery_1_power') %}
          {% set lim = states('input_number.dump_max_discharge_t2_w') | float(0) %}
          {{ is_number(p) and (p | float) >= -lim }}
      - name: Dump batt KU ok
        unique_id: dump_batt_ku_ok
        default_entity_id: binary_sensor.dump_batt_ku_ok
        state: >
          {% set p = states('sensor.battery_2_power') %}
          {% set lim = states('input_number.dump_max_discharge_ku_w') | float(0) %}
          {{ is_number(p) and (p | float) >= -lim }}
      - name: Dump batt SPH ok
        unique_id: dump_batt_sph_ok
        default_entity_id: binary_sensor.dump_batt_sph_ok
        state: >
          {% set a = states('sensor.sungold_sph302480a_battery_current') %}
          {% set v = states('sensor.sungold_sph302480a_battery_voltage') %}
          {% set lim = states('input_number.dump_max_discharge_sph_w') | float(50) %}
          {% if is_number(a) and is_number(v) %}
            {{ ((a | float) * (v | float)) >= -lim }}
          {% else %}
            {{ false }}
          {% endif %}
      - name: Dump charge float
        unique_id: dump_charge_float
        default_entity_id: binary_sensor.dump_charge_float
        state: >
          {% set a = states('sensor.solar_controller_charge_state') | lower %}
          {% set b = states('sensor.solar_controller_battery_state') | lower %}
          {{ a == 'float' or b == 'float' }}
      - name: Dump solar present
        unique_id: dump_solar_present
        default_entity_id: binary_sensor.dump_solar_present
        state: >
          {% set s = states('sensor.solar_controller_solar') %}
          {% set s2 = states('sensor.solar_controller_solar_power') %}
          {% set pv = s if is_number(s) else s2 %}
          {% set minw = states('input_number.dump_min_solar_w') | float(50) %}
          {{ is_number(pv) and (pv | float) >= minw }}
      - name: Dump SoC ok
        unique_id: dump_soc_ok
        default_entity_id: binary_sensor.dump_soc_ok
        state: >
          {% if is_state('input_boolean.dump_soc_unsynced', 'on') %}
            {{ true }}
          {% else %}
            {% set s = states('sensor.battery_1_state_of_charge') %}
            {% set m = states('input_number.dump_min_soc_percent') | float(95) %}
            {{ is_number(s) and (s | float) >= m }}
          {% endif %}
      - name: Dump T2 float hold
        unique_id: dump_v_float_t2
        default_entity_id: binary_sensor.dump_v_float_t2
        state: >
          {% set v = states('sensor.battery_1_voltage') %}
          {% set fl = states('input_number.dump_float_t2_v') %}
          {{ is_number(v) and is_number(fl) and (v | float) >= (fl | float) }}
      - name: Dump T2 re-bulk
        unique_id: dump_v_rebulk_t2
        default_entity_id: binary_sensor.dump_v_rebulk_t2
        state: >
          {% set v = states('sensor.battery_1_voltage') %}
          {% set rb = states('input_number.dump_rebulk_t2_v') %}
          {{ is_number(v) and is_number(rb) and (v | float) <= (rb | float) }}
      - name: Dump KU float hold
        unique_id: dump_v_float_ku
        default_entity_id: binary_sensor.dump_v_float_ku
        state: >
          {% set v = states('sensor.battery_2_voltage') %}
          {% set fl = states('input_number.dump_float_ku_v') %}
          {{ is_number(v) and is_number(fl) and (v | float) >= (fl | float) }}
      - name: Dump KU re-bulk
        unique_id: dump_v_rebulk_ku
        default_entity_id: binary_sensor.dump_v_rebulk_ku
        state: >
          {% set v = states('sensor.battery_2_voltage') %}
          {% set rb = states('input_number.dump_rebulk_ku_v') %}
          {{ is_number(v) and is_number(rb) and (v | float) <= (rb | float) }}
      - name: Dump SPH float hold
        unique_id: dump_v_float_sph
        default_entity_id: binary_sensor.dump_v_float_sph
        state: >
          {% set v = states('sensor.sungold_sph302480a_battery_voltage') %}
          {% set fl = states('input_number.dump_float_sph_v') %}
          {{ is_number(v) and is_number(fl) and (v | float) >= (fl | float) }}
      - name: Dump SPH re-bulk
        unique_id: dump_v_rebulk_sph
        default_entity_id: binary_sensor.dump_v_rebulk_sph
        state: >
          {% set v = states('sensor.sungold_sph302480a_battery_voltage') %}
          {% set rb = states('input_number.dump_rebulk_sph_v') %}
          {{ is_number(v) and is_number(rb) and (v | float) <= (rb | float) }}

sensor:
  - platform: derivative
    source: sensor.dump_surplus_w
    name: Dump surplus derivative
    unique_id: dump_surplus_derivative
    round: 1
    unit_time: min
    time_window: "00:15:00"

binary_sensor:
  - platform: threshold
    entity_id: sensor.dump_surplus_w
    name: Dump surplus high
    upper: 125
    hysteresis: 75
  - platform: threshold
    entity_id: sensor.dump_surplus_derivative
    name: Dump PV falling
    lower: 0
    hysteresis: 5

automation dump_load:
  - id: sim_dump_turn_on
    alias: Dump load turn on
    description: Victron float hold. Stage one plug, site load delta confirm, then another while the bus stays above re-bulk.
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.dump_charge_float
        to: "on"
        for: "00:01:00"
      - trigger: state
        entity_id: binary_sensor.dump_solar_present
        to: "on"
        for: "00:01:00"
      - trigger: state
        entity_id: sensor.dump_next_plug
    conditions:
      - condition: state
        entity_id: input_boolean.dump_control_enabled
        state: "on"
      - condition: state
        entity_id: binary_sensor.dump_charge_float
        state: "on"
        for:
          minutes: 1
      - condition: state
        entity_id: binary_sensor.dump_solar_present
        state: "on"
        for:
          minutes: 1
      - condition: state
        entity_id: binary_sensor.dump_soc_ok
        state: "on"
      - condition: not
        conditions:
          - condition: state
            entity_id: binary_sensor.dump_pv_falling
            state: "on"
      - condition: template
        value_template: "{{ states('sensor.dump_next_plug').startswith('switch.sim_ac_plug_') }}"
    actions:
      - repeat:
          while:
            - condition: state
              entity_id: binary_sensor.dump_charge_float
              state: "on"
            - condition: state
              entity_id: binary_sensor.dump_solar_present
              state: "on"
            - condition: state
              entity_id: binary_sensor.dump_soc_ok
              state: "on"
            - condition: not
              conditions:
                - condition: state
                  entity_id: binary_sensor.dump_pv_falling
                  state: "on"
            - condition: template
              value_template: "{{ states('sensor.dump_next_plug').startswith('switch.sim_ac_plug_') and repeat.index <= 6 }}"
          sequence:
            - variables:
                target: "{{ states('sensor.dump_next_plug') }}"
                plug_n: "{{ states('sensor.dump_next_plug').split('_')[-1] }}"
                bus: "{{ states('input_select.dump_plug_' ~ states('sensor.dump_next_plug').split('_')[-1] ~ '_inverter') }}"
            - variables:
                load_eid: "{{ 'sensor.dump_bus_load_t2' if bus == 'T2' else ('sensor.dump_bus_load_ku' if bus == 'KU' else 'sensor.dump_bus_load_sph') }}"
            - variables:
                load_before: "{{ states(load_eid) | float if is_number(states(load_eid)) else none }}"
            - condition: template
              value_template: "{{ target.startswith('switch.sim_ac_plug_') }}"
            - action: switch.turn_on
              target:
                entity_id: "{{ target }}"
            - delay: "{{ states('input_number.dump_site_confirm_s') | int(5) }}"
            - variables:
                load_after: "{{ states(load_eid) | float if is_number(states(load_eid)) else none }}"
                plug_pwr: "{{ states('sensor.' ~ target[7:] ~ '_power') }}"
                delta_min: "{{ states('input_number.dump_site_delta_min_w') | float(25) }}"
            - variables:
                confirmed: >
                  {{ (is_number(plug_pwr) and (plug_pwr | float) > 0)
                     or (is_number(load_before) and is_number(load_after)
                         and (load_after | float - load_before | float) >= (delta_min | float)) }}
            - if:
                - condition: template
                  value_template: >
                    {{ not is_state('binary_sensor.dump_charge_float', 'on')
                       or not is_state('binary_sensor.dump_solar_present', 'on') }}
              then:
                - action: switch.turn_off
                  target:
                    entity_id: "{{ target }}"
                - action: timer.start
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_cooldown"
                - action: timer.cancel
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_min_on"
                - stop: left float or solar gone
            - if:
                - condition: template
                  value_template: "{{ not confirmed }}"
              then:
                - action: switch.turn_off
                  target:
                    entity_id: "{{ target }}"
                - action: timer.start
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_cooldown"
                - delay: 1
              else:
                - action: timer.start
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_min_on"
                - delay: 1

  - id: sim_dump_turn_off_solar_gone
    alias: Dump load turn off solar gone
    description: PV below dump_min_solar_w for 1 minute (weather or end of day). Cancel min-on so dumps are not night loads (95%+ leftover).
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.dump_solar_present
        to: "off"
        for: "00:01:00"
    conditions:
      - condition: state
        entity_id: input_boolean.dump_control_enabled
        state: "on"
    actions:
      - action: switch.turn_off
        target:
          entity_id:
            - switch.sim_ac_plug_1
            - switch.sim_ac_plug_2
            - switch.sim_ac_plug_3
            - switch.sim_ac_plug_4
            - switch.sim_ac_plug_5
            - switch.sim_ac_plug_6
      - action: timer.cancel
        target:
          entity_id:
            - timer.dump_plug_1_min_on
            - timer.dump_plug_2_min_on
            - timer.dump_plug_3_min_on
            - timer.dump_plug_4_min_on
            - timer.dump_plug_5_min_on
            - timer.dump_plug_6_min_on
      - action: timer.start
        target:
          entity_id:
            - timer.dump_plug_1_cooldown
            - timer.dump_plug_2_cooldown
            - timer.dump_plug_3_cooldown
            - timer.dump_plug_4_cooldown
            - timer.dump_plug_5_cooldown
            - timer.dump_plug_6_cooldown

  - id: sim_dump_turn_off_bulk
    alias: Dump load turn off bulk
    description: Battery wins. Charge not absorption/float for 1 minute (Victron re-bulk scale) turns dumps off even during min-on.
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.dump_charge_ok
        to: "off"
        for: "00:01:00"
    conditions:
      - condition: state
        entity_id: input_boolean.dump_control_enabled
        state: "on"
    actions:
      - action: switch.turn_off
        target:
          entity_id:
            - switch.sim_ac_plug_1
            - switch.sim_ac_plug_2
            - switch.sim_ac_plug_3
            - switch.sim_ac_plug_4
            - switch.sim_ac_plug_5
            - switch.sim_ac_plug_6
      - action: timer.cancel
        target:
          entity_id:
            - timer.dump_plug_1_min_on
            - timer.dump_plug_2_min_on
            - timer.dump_plug_3_min_on
            - timer.dump_plug_4_min_on
            - timer.dump_plug_5_min_on
            - timer.dump_plug_6_min_on
      - action: timer.start
        target:
          entity_id:
            - timer.dump_plug_1_cooldown
            - timer.dump_plug_2_cooldown
            - timer.dump_plug_3_cooldown
            - timer.dump_plug_4_cooldown
            - timer.dump_plug_5_cooldown
            - timer.dump_plug_6_cooldown

  - id: sim_dump_turn_off_rebulk_t2
    alias: Dump load turn off T2 re-bulk
    description: T2 bus voltage at or below dump_rebulk_t2_v for 1 minute (Victron LiFePO4 24 V offset). Turn off T2 plugs even during min-on.
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.dump_v_rebulk_t2
        to: "on"
        for: "00:01:00"
    conditions:
      - condition: state
        entity_id: input_boolean.dump_control_enabled
        state: "on"
    actions:
      - repeat:
          for_each:
            - plug: switch.sim_ac_plug_1
              select: input_select.dump_plug_1_inverter
            - plug: switch.sim_ac_plug_2
              select: input_select.dump_plug_2_inverter
            - plug: switch.sim_ac_plug_3
              select: input_select.dump_plug_3_inverter
            - plug: switch.sim_ac_plug_4
              select: input_select.dump_plug_4_inverter
            - plug: switch.sim_ac_plug_5
              select: input_select.dump_plug_5_inverter
            - plug: switch.sim_ac_plug_6
              select: input_select.dump_plug_6_inverter
          sequence:
            - if:
                - condition: template
                  value_template: "{{ is_state(repeat.item.select, 'T2') }}"
              then:
                - action: switch.turn_off
                  target:
                    entity_id: "{{ repeat.item.plug }}"
                - variables:
                    plug_n: "{{ repeat.item.plug.split('_')[-1] }}"
                - action: timer.cancel
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_min_on"
                - action: timer.start
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_cooldown"

  - id: sim_dump_turn_off_rebulk_ku
    alias: Dump load turn off KU re-bulk
    description: KU bus voltage at or below dump_rebulk_ku_v for 1 minute. Turn off KU plugs even during min-on.
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.dump_v_rebulk_ku
        to: "on"
        for: "00:01:00"
    conditions:
      - condition: state
        entity_id: input_boolean.dump_control_enabled
        state: "on"
    actions:
      - repeat:
          for_each:
            - plug: switch.sim_ac_plug_1
              select: input_select.dump_plug_1_inverter
            - plug: switch.sim_ac_plug_2
              select: input_select.dump_plug_2_inverter
            - plug: switch.sim_ac_plug_3
              select: input_select.dump_plug_3_inverter
            - plug: switch.sim_ac_plug_4
              select: input_select.dump_plug_4_inverter
            - plug: switch.sim_ac_plug_5
              select: input_select.dump_plug_5_inverter
            - plug: switch.sim_ac_plug_6
              select: input_select.dump_plug_6_inverter
          sequence:
            - if:
                - condition: template
                  value_template: "{{ is_state(repeat.item.select, 'KU') }}"
              then:
                - action: switch.turn_off
                  target:
                    entity_id: "{{ repeat.item.plug }}"
                - variables:
                    plug_n: "{{ repeat.item.plug.split('_')[-1] }}"
                - action: timer.cancel
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_min_on"
                - action: timer.start
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_cooldown"

  - id: sim_dump_turn_off_rebulk_sph
    alias: Dump load turn off SPH re-bulk
    description: SPH cart voltage at or below dump_rebulk_sph_v for 1 minute. Turn off SPH plugs even during min-on.
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.dump_v_rebulk_sph
        to: "on"
        for: "00:01:00"
    conditions:
      - condition: state
        entity_id: input_boolean.dump_control_enabled
        state: "on"
    actions:
      - repeat:
          for_each:
            - plug: switch.sim_ac_plug_1
              select: input_select.dump_plug_1_inverter
            - plug: switch.sim_ac_plug_2
              select: input_select.dump_plug_2_inverter
            - plug: switch.sim_ac_plug_3
              select: input_select.dump_plug_3_inverter
            - plug: switch.sim_ac_plug_4
              select: input_select.dump_plug_4_inverter
            - plug: switch.sim_ac_plug_5
              select: input_select.dump_plug_5_inverter
            - plug: switch.sim_ac_plug_6
              select: input_select.dump_plug_6_inverter
          sequence:
            - if:
                - condition: template
                  value_template: "{{ is_state(repeat.item.select, 'SPH') }}"
              then:
                - action: switch.turn_off
                  target:
                    entity_id: "{{ repeat.item.plug }}"
                - variables:
                    plug_n: "{{ repeat.item.plug.split('_')[-1] }}"
                - action: timer.cancel
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_min_on"
                - action: timer.start
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_cooldown"

  - id: sim_dump_turn_off_batt_t2
    alias: Dump load turn off T2 battery
    description: T2 battery discharging beyond helper. Turn off T2-assigned plugs even during min-on.
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.dump_batt_t2_ok
        to: "off"
        for: "00:01:00"
    conditions:
      - condition: state
        entity_id: input_boolean.dump_control_enabled
        state: "on"
    actions:
      - repeat:
          for_each:
            - plug: switch.sim_ac_plug_1
              select: input_select.dump_plug_1_inverter
            - plug: switch.sim_ac_plug_2
              select: input_select.dump_plug_2_inverter
            - plug: switch.sim_ac_plug_3
              select: input_select.dump_plug_3_inverter
            - plug: switch.sim_ac_plug_4
              select: input_select.dump_plug_4_inverter
            - plug: switch.sim_ac_plug_5
              select: input_select.dump_plug_5_inverter
            - plug: switch.sim_ac_plug_6
              select: input_select.dump_plug_6_inverter
          sequence:
            - if:
                - condition: template
                  value_template: "{{ is_state(repeat.item.select, 'T2') }}"
              then:
                - action: switch.turn_off
                  target:
                    entity_id: "{{ repeat.item.plug }}"
                - variables:
                    plug_n: "{{ repeat.item.plug.split('_')[-1] }}"
                - action: timer.cancel
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_min_on"
                - action: timer.start
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_cooldown"

  - id: sim_dump_turn_off_batt_ku
    alias: Dump load turn off KU battery
    description: KU battery discharging beyond helper. Turn off KU-assigned plugs even during min-on.
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.dump_batt_ku_ok
        to: "off"
        for: "00:01:00"
    conditions:
      - condition: state
        entity_id: input_boolean.dump_control_enabled
        state: "on"
    actions:
      - repeat:
          for_each:
            - plug: switch.sim_ac_plug_1
              select: input_select.dump_plug_1_inverter
            - plug: switch.sim_ac_plug_2
              select: input_select.dump_plug_2_inverter
            - plug: switch.sim_ac_plug_3
              select: input_select.dump_plug_3_inverter
            - plug: switch.sim_ac_plug_4
              select: input_select.dump_plug_4_inverter
            - plug: switch.sim_ac_plug_5
              select: input_select.dump_plug_5_inverter
            - plug: switch.sim_ac_plug_6
              select: input_select.dump_plug_6_inverter
          sequence:
            - if:
                - condition: template
                  value_template: "{{ is_state(repeat.item.select, 'KU') }}"
              then:
                - action: switch.turn_off
                  target:
                    entity_id: "{{ repeat.item.plug }}"
                - variables:
                    plug_n: "{{ repeat.item.plug.split('_')[-1] }}"
                - action: timer.cancel
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_min_on"
                - action: timer.start
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_cooldown"

  - id: sim_dump_turn_off_batt_sph
    alias: Dump load turn off SPH battery
    description: SPH cart battery discharging beyond helper. Turn off SPH-assigned plugs even during min-on.
    mode: single
    triggers:
      - trigger: state
        entity_id: binary_sensor.dump_batt_sph_ok
        to: "off"
        for: "00:01:00"
    conditions:
      - condition: state
        entity_id: input_boolean.dump_control_enabled
        state: "on"
    actions:
      - repeat:
          for_each:
            - plug: switch.sim_ac_plug_1
              select: input_select.dump_plug_1_inverter
            - plug: switch.sim_ac_plug_2
              select: input_select.dump_plug_2_inverter
            - plug: switch.sim_ac_plug_3
              select: input_select.dump_plug_3_inverter
            - plug: switch.sim_ac_plug_4
              select: input_select.dump_plug_4_inverter
            - plug: switch.sim_ac_plug_5
              select: input_select.dump_plug_5_inverter
            - plug: switch.sim_ac_plug_6
              select: input_select.dump_plug_6_inverter
          sequence:
            - if:
                - condition: template
                  value_template: "{{ is_state(repeat.item.select, 'SPH') }}"
              then:
                - action: switch.turn_off
                  target:
                    entity_id: "{{ repeat.item.plug }}"
                - variables:
                    plug_n: "{{ repeat.item.plug.split('_')[-1] }}"
                - action: timer.cancel
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_min_on"
                - action: timer.start
                  target:
                    entity_id: "timer.dump_plug_{{ plug_n }}_cooldown"
